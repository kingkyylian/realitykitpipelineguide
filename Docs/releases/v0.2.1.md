# v0.2.1 - Release Identity and Screenshot Evidence Patch

Status: Draft patch release notes. Do not tag or publish until `main` is pushed and GitHub Actions passes.

Target tag: `v0.2.1`

Compare range after tag: `v0.2.0..v0.2.1`

## Summary

This patch candidate fixes two issues found during realistic tool evaluation of the published `v0.2.0` release.

## Fixed

- Bumped package/runtime version metadata from `0.1.0` to `0.2.1` so `rkp --version`, wheel metadata, and the release tag can line up again.
- Hardened `rkp accept-asset` so screenshot evidence must be a PNG or JPEG image by file header.
- Added regression coverage for package metadata, `rkp --version`, and non-image screenshot rejection.

## Verification

```text
/private/tmp/rkp-install-v020-a/bin/rkp --version: rkp 0.1.0, confirms published v0.2.0 mismatch
/private/tmp/rkp-install-local-021-a/bin/rkp --version: rkp 0.2.1
/private/tmp/rkp-install-local-021-a/bin/rkp accept-asset patch_drone --screenshot Docs/screenshots/not_an_image.jpg: rejected as not a valid PNG or JPEG image
/private/tmp/rkp-install-local-021-a/bin/rkp release-check --assets: ok in external project
rtk .venv/bin/python -m unittest discover -s Tests: ok, 155 tests
rtk make verify-local: ok
rtk .venv/bin/python Tools/rkp.py release-check --assets: ok
rtk .venv/bin/python Tools/rkp.py release-check: ok
```

## Publish Checklist

- [ ] Commit the patch candidate.
- [ ] Push `main` only after explicit user approval.
- [ ] Wait for GitHub Actions to pass on the pushed commit.
- [ ] Create and push `v0.2.1`.
- [ ] Publish the GitHub Release from this file.
