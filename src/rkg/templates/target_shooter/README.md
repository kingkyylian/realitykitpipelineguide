# Target Shooter Template

The first target-shooter template is generated by `rkg init-game`.

It starts with a fixed non-AR RealityKit camera, procedural floor, procedural target, simple HUD, session metadata, and RKP asset manifest entries. Keep imported target art planned until RealityKit screenshot acceptance.
