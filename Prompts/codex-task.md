# Codex Task Prompt

Use this when assigning implementation work.

```text
In /Users/kyylian/Developer/RealityKitPipelineDemo, implement the next small RealityKit feature.

Goal:

Constraints:
- Keep the app compiling.
- Keep changes scoped.
- Follow existing SwiftUI + GameARView structure.
- Update docs if the pipeline changes.

Verification:
- Run xcodegen if project.yml changed.
- Run xcodebuild for iOS simulator.
- Report changed files and remaining risks.
```
